# Optional: the application works offline without this file.
APP_MODE=offline
PORT=3001
FRONTEND_PORT=5173

# Overrides are normally unnecessary. See backend/config.json for defaults.
# CATALOG_PATH=data/catalog.json
# FRONTEND_DIST_PATH=../frontend/dist
# CART_URL=/cart
# VITE_API_BASE_URL=/api

# Optional live adapters. Never commit populated credentials.
# NVIDIA_API_KEY=
# NVIDIA_MODEL=meta/llama-3.1-8b-instruct
# EKT_API_USERNAME=
# EKT_API_PASSWORD=
